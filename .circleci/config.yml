version: 2.1

jobs:
  avx2_probe:
    docker:
      - image: gcc:13
    steps:
      - checkout
      - run:
          name: Run AVX2 full probe
          command: bash scripts/run_probe.sh
      - store_artifacts:
          path: out

workflows:
  avx2:
    jobs:
      - avx2_probe
